# React typescript component loader

## Short description

A high-level component that displays a spinner until the correct data is loaded. The project was styled using the Bootstrap freamework and React-bootstrap library.